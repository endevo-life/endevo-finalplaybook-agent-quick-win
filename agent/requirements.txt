anthropic
pydantic
fastapi
uvicorn
pytest
httpx
python-dotenv
boto3
mangum        # ASGI adapter: run the FastAPI app inside AWS Lambda
stripe        # paid-tier checkout + webhooks (optional at runtime; see billing.py)

anthropic
pydantic
fastapi
uvicorn
pytest
httpx
python-dotenv
boto3
mangum        # ASGI adapter: run the FastAPI app inside AWS Lambda
stripe        # paid-tier checkout + webhooks (optional at runtime; see billing.py)
tzdata        # IANA timezones for zoneinfo. Linux/Lambda images ship NO tz
              # database, so without this the weekly digest's Mon-Sun window
              # silently falls back to UTC and reports the wrong 7 days.
